# GitHub 个人访问令牌（用于数据持久化）
# 将此文件复制为 secrets.toml 并填入你的 token
# 获取方式：GitHub → Settings → Developer settings → Personal access tokens → Fine-grained tokens
# 权限要求：Contents (Read and write)
# 注意：secrets.toml 已在 .gitignore 中排除，不会提交到仓库
GITHUB_TOKEN = "你的GitHub个人访问令牌"
